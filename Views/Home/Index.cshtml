@{
    ViewData["Title"] = "Home";
}

<div class="container mt-5">
    <div class="text-center mb-5">
        <h1 class="display-5 fw-bold">BeReadyForExam</h1>
        <p class="lead">Interactive exam and quiz platform</p>
    </div>

    @if (User.Identity != null && User.Identity.IsAuthenticated)
    {
            <div class="row g-3 justify-content-center">
            @if (User.IsInRole("Teacher") || User.IsInRole("Admin"))
            {
                        <div class="col-md-4">
                            <a asp-controller="Teacher" asp-action="Dashboard" class="btn btn-primary w-100 py-3">
                                Teacher Dashboard
                            </a>
                        </div>

                        <div class="col-md-4">
                            <a asp-controller="Exam" asp-action="Index" class="btn btn-outline-primary w-100 py-3">
                                Manage Exams
                            </a>
                        </div>

                        <div class="col-md-4">
                            <a asp-controller="Question" asp-action="Index" class="btn btn-outline-secondary w-100 py-3">
                                Manage Questions
                            </a>
                        </div>

                        <div class="col-md-4">
                            <a asp-controller="Topics" asp-action="Index" class="btn btn-outline-dark w-100 py-3">
                                Manage Topics
                            </a>
                        </div>

                        <div class="col-md-4">
                            <a asp-controller="Subjects" asp-action="Index" class="btn btn-outline-dark w-100 py-3">
                                Manage Subjects
                            </a>
                        </div>
            }

                <div class="col-md-4">
                    <a asp-controller="Exam" asp-action="Available" class="btn btn-success w-100 py-3">
                        Available Exams
                    </a>
                </div>

                <div class="col-md-4">
                    <a asp-controller="Exam" asp-action="MyHistory" class="btn btn-outline-success w-100 py-3">
                        My History
                    </a>
                </div>
            </div>
    }
    else
    {
            <div class="text-center">
                <p class="mb-3">Please log in to start using the platform.</p>
                <a asp-area="Identity" asp-page="/Account/Login" class="btn btn-primary me-2">Login</a>
                <a asp-area="Identity" asp-page="/Account/Register" class="btn btn-outline-primary">Register</a>
            </div>
    }
</div>